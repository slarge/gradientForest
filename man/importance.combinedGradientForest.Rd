\name{importance.combinedGradientForest}
\Rdversion{1.1}
\alias{importance.combinedGradientForest}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Importance of predictors or species for a combinedGradientForest object
}
\description{
Provide access to the importance values of predictors or species
}
\usage{
\method{importance}{combinedGradientForest}(x, type = c("Weighted", "Raw", "Species")[1], sort = T, ...)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{x}{
      an object of class "combinedGradientForest" generated by \code{\link{combinedGradientForest}}.
  }
  \item{type}{
    type of importance measure. The choices are
    \itemize{
	  \item{Weighted}{measure is an average of accuracy importance weighted by species \eqn{R^2}}
	  \item{Raw}{measure is a simple average of accuracy importance over species}
	  \item{Species}{measure is the species \eqn{R^2}}
	  }
  }
  \item{sort}{
    if TRUE, sort by decreasing importance.
  }
  \item{\ldots}{further arguments passed to or from other methods.}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
  a named vector of importance measures.
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
    N. Ellis, CSIRO, Cleveland, Australia. <Nick.Ellis@csiro.au>.
    S.J. Smith, DFO, Dartmouth, NS, Canada. <Stephen.Smith@dfo-mpo.gc.ca>
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
 \code{\link{importance.gradientForest}}
}
\examples{
data(CoMLsimulation)
preds <- colnames(Xsimulation)
specs <- colnames(Ysimulation)
f1 <- gradientForest(data.frame(Ysimulation,Xsimulation), preds, specs[1:6], ntree=10)
f2 <- gradientForest(data.frame(Ysimulation,Xsimulation), preds, specs[1:6+6], ntree=10)
f12 <- combinedGradientForest(west=f1,east=f2)
importance(f12)
importance(f12, type = "Species", sort = FALSE)
}

% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line