\name{rename.predictors}
\alias{rename.predictors}
\title{
Rename predictor variables
}
\description{
Utility function to rename predictor variables in a gradientForest object.
}
\usage{
rename.predictors(obj, old.names, new.names)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{obj}{
an object of class "gradientForest" generated by \code{\link{gradientForest}}
}
  \item{old.names}{
character vector of current variable names to be changed.
}
  \item{new.names}{
character vector of new variable names.    Note that new.names must have same length as old.names.
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
Returns  a  \code{\link{gradientForest}} object with predictor variables renamed in all components containing the predictor variables.
}
\references{

}
\author{
N. Ellis, CSIRO, Cleveland, Australia. <Nick.Ellis@csiro.au>
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
\code{\link{gradientForest}}
}
\examples{
data(CoMLsimulation)
preds <- colnames(Xsimulation)
specs <- colnames(Ysimulation)
f1 <- gradientForest(data.frame(Ysimulation,Xsimulation), preds, specs, ntree=10)
f1
f1.new<-rename.predictors(f1,names(preds),Newnames)
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ utilities }
\keyword{ misc }% __ONLY ONE__ keyword per line
